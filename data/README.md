# Data Directory

This directory contains track data and telemetry logs.

## Structure

- `raw/` - Raw sensor data and telemetry
- `processed/` - Preprocessed data ready for training

## Data Format

Data files should follow these conventions:
- Sensor data: CSV or HDF5 format
- Track files: JSON format with coordinates
- Telemetry logs: Timestamped CSV files
